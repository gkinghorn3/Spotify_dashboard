import { css } from 'styled-components';

const fonts = css`
  @font-face {
    font-family: 'Mulish';
    src: url('public/fonts/Mulish.woff2') format('woff2-variations'),
         url('public/fonts/Mulish.woff2') format('woff2');
    font-weight: 100 900;
    font-style: normal;
  }
`;

export default fonts;